# Variables for the config

# export JIRA_URL=my_jira_url.com/ # for 'ticket' aliases
# export VIM_MODE=1 # to enable vim mode in zsh
# export NERD_FONT=1 # enable nerd font config for starship (run _gen_starship after configuring)
